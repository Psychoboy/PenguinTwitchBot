This directory holds locally-packed NuGet packages for development.
Populate it with your own pack scripts or dotnet pack commands as needed.
Add LocalPackages/*.nupkg to .gitignore to avoid committing build artifacts.
